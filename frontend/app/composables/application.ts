import type {Application} from "@/composables/types";

// Application global variables forms up in "/app/script.ts" file.
// Type "Application" defined in "/composables/types.ts".
const application: Application = {} as Application;

export default application;
