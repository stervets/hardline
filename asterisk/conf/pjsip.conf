[transport-udp]
type=transport
protocol=udp
bind=0.0.0.0:5060
; если за NAT — asterisk сам увидит внешний по contact/rtp_symmetric,
; но для продакшна лучше явно external_* и local_net

[global]
type=global
user_agent=Hardline-Asterisk

; ---- шаблоны (будем генерить runtime через realtime/конфиг later)
; пока оставляем пусто: backend будет создавать endpoint/auth/aor через ARI/CLI/конфиг (решим)
